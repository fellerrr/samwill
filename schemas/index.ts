import social from "./social";
import pageInfo from "./pafeInfo";
import advantages from "@/schemas/advantages";
import sale from "@/schemas/sale";
import demand from "@/schemas/demand";

export const schemaTypes = [social, pageInfo, advantages, demand, sale]
